import { element } from 'base/base'

export default element
  .config({ name: 'Heading/Wrapper' })
  .attrs({
    contentDirection: 'rows',
    contentAlignX: 'center',
  })
  .theme({
    height: 700,
  })
